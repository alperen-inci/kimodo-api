# Kimodo API runtime dependencies.
# torch and kimodo are expected to be pre-installed in the Docker image.
fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.6
pydantic>=2.0
numpy>=1.23
einops>=0.7
